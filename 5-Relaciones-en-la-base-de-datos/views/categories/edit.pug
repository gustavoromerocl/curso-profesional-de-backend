doctype html 
html 
    head
        title Categorias
    body
        form(action=`/categories/${category.id}?_method=PUT` method="POST") 
            input(type="text" name="title" placeholder="titulo" value=`${category.title}`)
            input(type="color" name="color" placeholder="color" value=`${category.color}`)
            input(type="submit" value="Actualizar")